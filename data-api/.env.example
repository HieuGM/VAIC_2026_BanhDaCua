# PostgreSQL data source
DB_HOST=localhost
DB_PORT=5432
DB=hanoi_heart
DB_USER=hanoiheart
DB_PASS=hanoiheart

# Server
SERVER_PORT=8081

# CORS allowed origin (frontend dev server)
CORS_ALLOWED_ORIGINS=http://localhost:3000

# Chatbot-service (FastAPI) base URL — data-api proxies POST /api/v1/chat
CHATBOT_BASE_URL=http://localhost:8000
CHATBOT_CONNECT_TIMEOUT_MS=10000
CHATBOT_READ_TIMEOUT_MS=10000
