# PostgreSQL data source
DB_HOST=localhost
DB_PORT=5432
DB=hanoi_heart
DB_USER=hanoiheart
DB_PASS=hanoiheart

# Server
SERVER_PORT=8081

# CORS allowed origin (frontend dev server)
CORS_ALLOWED_ORIGINS=http://localhost:3000

# Demo API key (X-API-Key header). Leave empty to disable.
DATA_API_KEY=
